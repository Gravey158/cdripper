build/
# Alle Build-Verzeichnisse, auch beiseitegelegte (build-mac.old o. ä.) —
# ein solcher Ordner hat sich schon einmal ueber `git add -A` mit 739
# Dateien in einen Commit geschmuggelt.
build-*/
config.ini
*.o
cdripper

# Sparkle (fetched by scripts/fetch-sparkle.sh, ~10MB Framework)
mac/Sparkle.framework/
mac/sparkle-tools/
mac/sparkle-version

# AppImage build outputs
*.AppImage
*.AppDir/
